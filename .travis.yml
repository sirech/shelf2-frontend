language: node_js
node_js:
  - 8
cache:
  yarn: true
  directories:
    - node_modules
jobs:
  include:
    - stage: 'Flow'
      script: npm run flow
    - stage: 'Test'
      script: npm test -- --coverage
